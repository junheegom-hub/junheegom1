- name: Build EXE
  shell: powershell
  run: |
    @"
    {
      "name": "app",
      "version": "1.0.0",
      "main": "main.js",
      "scripts": {
        "build": "electron-builder"
      }
    }
    "@ | Out-File -Encoding utf8 package.json
